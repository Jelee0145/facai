# 代码审查修复项汇总

## 09-backend-key-manager (`backend/key_manager.py`)

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 1 | Critical | 轮询索引越界：Key 被禁用后 `_current_index` 可能超出新列表长度 | `key_manager.py:25` | 先 clamp 索引再访问：`self._current_index %= len(keys)` 然后 `return keys[self._current_index]`，再递增 | 待修复 |
| 2 | Critical | `threading.Lock` 在 asyncio 环境下语义不正确 | `key_manager.py:15` | 当前单线程 asyncio 下碰巧可工作，加注释说明前提；未来多 worker 时改 `asyncio.Lock` | 待评估 |
| 3 | High | `mark_success` 清零 `fail_count`，不稳定 Key 永远不会被禁用 | `database.py:324` | 改为衰减：`fail_count = MAX(fail_count - 1, 0)` 而非 `fail_count = 0` | 待修复 |
| 4 | High | `mark_failure` 不区分认证失败与网络错误 | `key_manager.py:36-40` | 新增 `network_fail_count` 字段，网络错误单独计数，连续网络错误时也触发 Key 切换 | 待评估 |
| 5 | Medium | `health_check()` 不包含被禁用的 Key | `key_manager.py:42-62` | 改用 `get_all_keys()` 并标注各 Key 的 active 状态，增加 `disabled_count` 字段 | 待修复 |
| 6 | Medium | `reset_daily_usage()` 未见调用方 | `database.py:344-347` | 确认有定时任务（cron/APScheduler）每日调用，或在 `main.py` 启动时检查并执行重置 | 待确认 |
| 7 | Low | `get_active_key` 返回 `dict` 无类型定义 | `key_manager.py:18` | 定义 `TypedDict` 或 `dataclass` 替代原始 dict | 待修复 |
| 8 | Low | 无单元测试覆盖 | - | 补充并发场景、故障转移、限额溢出等测试 | 待修复 |
| 9 | Low | 全局单例 `key_manager` 无懒加载保护，模块导入时创建 | `key_manager.py:65-66` | 使用 `functools.lru_cache` 或显式 `get_key_manager()` 函数确保单例 | 待评估 |

---

## 10-backend-sse (`backend/sse.py`)

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 10 | High | `defaultdict(list)` 迭代安全问题：并发 subscribe/unsubscribe 可能导致 RuntimeError | `sse.py:19` | 使用 `list(_subscribers.get(task_id, []))` 复制后遍历 | 待修复 |
| 11 | High | 缺少过期 task_id 的清理机制 | `sse.py:7` | 为每个 task_id 添加时间戳，定期清理超过 N 分钟无活动的条目；或在 `push_event` 中检测到空列表时立即清理 | 待修复 |
| 12 | Medium | `push_event` 同步函数在异步环境中的适配 | `sse.py:17` | 保持当前设计（`put_nowait` 本身就是非阻塞的），但需确保所有调用都在同一 asyncio event loop 中 | 待评估 |
| 13 | Medium | QueueFull 时静默丢弃事件 | `sse.py:22-23` | 考虑使用无界队列或设置较大容量（如 100）；如果保留有界队列，至少对 `completed`/`failed` 事件使用 `await q.put()` 阻塞等待 | 待修复 |
| 14 | Medium | 300 秒超时硬编码 | `sse.py:35` | 将超时时间改为可配置参数，或根据任务图片数量动态调整 | 待修复 |
| 15 | Medium | 缺少并发安全锁保护 | `sse.py:7-32` | 引入 `asyncio.Lock` 保护 `subscribe`/`unsubscribe` 操作 | 待评估 |
| 16 | Low | 缺少日志和可观测性 | `sse.py:1-50` | 在 `subscribe`/`unsubscribe` 中添加 debug 日志；在 `push_event` 中记录队列满事件 | 待修复 |
| 17 | Low | 缺少单元测试 | - | 编写测试覆盖基本订阅/推送/取消订阅流程、多订阅者并发接收、队列满时的行为 | 待修复 |
| 18 | Low | 类型标注不完整 | `sse.py:12` | 修改为 `asyncio.Queue[dict]`，增强类型安全性 | 待修复 |

---

## 04-frontend-infrastructure (前端基础设施层)

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 19 | Critical | Production CSP connect-src 缺失后端 URL | `middleware.ts:10` | 生产环境需明确 `connect-src 'self' ${backendUrl}`，或确认所有请求都走代理 | 待修复 |
| 20 | High | SSE 重连计数器重置时机不当 | `use-sse.ts:40` | 在 `source.onopen` 中重置 `reconnectCount = 0`，而非 `onmessage` | 待修复 |
| 21 | High | 缺少 uncaughtException 处理 | `server.ts:54-56` | 添加 `process.on('uncaughtException', ...)` 并触发关机流程 | 待修复 |
| 22 | Medium | img-src 缺少外部图片域名 | `middleware.ts:18` | 添加 `img-src 'self' data: blob: https:` 或具体域名白名单 | 待修复 |
| 23 | Medium | 缺少 upgrade-insecure-requests | `middleware.ts:14-25` | 生产环境 CSP 添加 `upgrade-insecure-requests` | 待修复 |
| 24 | Medium | 缺少 SSE onopen 回调 | `use-sse.ts:27-73` | 添加 `source.onopen` 处理，可选地通知调用方连接已建立 | 待修复 |
| 25 | Medium | handlersRef 模式存在潜在竞态 | `use-sse.ts:13-17` | 添加 `useMemo` 包装 handlers；或添加依赖数组 `[handlers]` 并比较 shallow | 待评估 |
| 26 | Low | X-XSS-Protection 未设置 | `middleware.ts:27-35` | 添加 `headers.set("X-XSS-Protection", "0")` | 待修复 |
| 27 | Low | parse error 被静默忽略 | `use-sse.ts:53-55` | 添加 `console.warn` 或 `logger.warn` 记录解析错误 | 待修复 |
| 28 | Low | 5 秒关机超时不可配置 | `server.ts:46-49` | 通过环境变量 `SHUTDOWN_TIMEOUT` 配置 | 待修复 |
| 29 | Low | 关机时未主动拒绝新连接 | `server.ts:38-50` | 可选地在关机时返回 503 给新请求 | 待评估 |
| 30 | Low | 缺少日志级别控制 | `logger.ts` | 通过环境变量 `LOG_LEVEL` 控制输出级别 | 待修复 |

---

## 02-frontend-main-page (`src/app/page.tsx`)

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 31 | High | 单文件 1600+ 行，30+ useState，职责严重过载 | `page.tsx:1-1613` | 拆分为 ProductTypeSelector、ImageUploader、GenerationProgress、ResultGallery、AuthModal、BillingModal、HistoryModal、TrendingModal、Lightbox 等子组件 | 待修复 |
| 32 | High | 状态分散为 30+ 独立 useState，用户/钱包/生成/图片/SSE 混杂 | `page.tsx:169-244` | 按功能域拆分自定义 Hook：useAuth、useGeneration、useBilling、useImageUpload；相关状态合并为对象 | 待修复 |
| 33 | High | filteredProducts/currentCountry/categories 每次渲染重新计算，getEffectiveProductType 每次创建新引用 | `page.tsx:327-331,773` | 用 useMemo 缓存计算结果，useCallback 缓存函数引用 | 待修复 |
| 34 | Medium | elapsedSeconds 初始化为 0 但从未递增，UI 计时器始终显示 "0秒" | `page.tsx:227,499,1091` | handleGenerate 中启动 setInterval 递增，onComplete/onError 中 clearInterval | 待修复 |
| 35 | Medium | 错误消息直接拼接后端 data.detail/error.message，暴露技术细节 | `page.tsx:323,528,577` | 对用户展示友好通用提示，技术细节仅记录 logger | 待修复 |
| 36 | Medium | "14张图"/"11种风格" 等数字在多处硬编码 | `page.tsx:226,288,499,1114` | 提取为 const TOTAL_GENERATED_IMAGES = 14, TOTAL_STYLES = 11 | 待修复 |
| 37 | Medium | generationStatus 使用原始字符串，无类型安全 | `page.tsx:228,313,502,524` | 定义 type GenerationStatus = 'idle' \| 'submitting' \| 'generating' \| 'completed' \| 'error' | 待修复 |
| 38 | Medium | Lightbox 仅支持点击关闭，无键盘导航 | `page.tsx:1581-1601` | 添加 useEffect 监听 Escape 键关闭，可选左右箭头切换图片 | 待修复 |
| 39 | Medium | 登录/注册表单 input 只有 placeholder，缺少 aria-label | `page.tsx:1426-1465` | 为每个 input 添加 aria-label 属性 | 待修复 |
| 40 | Low | COUNTRIES 中 platform 字段在整个文件中未使用 | `page.tsx:107-117` | 移除未使用的 platform 字段，或在 UI 中展示 | 待修复 |
| 41 | Low | ImageGallery 组件已定义但未被 page.tsx 使用 | `image-gallery.tsx` | 使用 ImageGallery 组件替换内联逻辑，或删除未使用组件 | 待修复 |
| 42 | Low | text-white/60 等半透明文字在深紫背景上，对比度可能低于 WCAG AA 标准 | `page.tsx` 全局样式 | 使用工具检查对比度，确保正文至少 4.5:1 | 待修复 |

---

## 01-frontend-api-proxy (`src/lib/proxy.ts` 等)

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 43 | Critical | SSRF TOCTOU 竞态条件：DNS 验证与实际 fetch 之间存在时间窗口 | `proxy-image/route.ts:43-91` | 使用 undici DNS pinning 或 `--experimental-fetch-dns` 在连接建立时持续验证 IP | 待修复 |
| 44 | Critical | 云元数据端点覆盖不完整：遗漏 `metadata.azure.internal`、`metadata` | `proxy-image/route.ts:30` | 扩展 `BLOCKED_HOSTNAMES` 添加 `metadata.azure.internal`、`metadata`、`metadata.google` | 待修复 |
| 45 | Critical | DNS 解析失败静默忽略：攻击者可构造解析失败域名绕过防护 | `proxy-image/route.ts:44-63` | DNS 解析失败时返回错误拒绝请求，或添加白名单机制 | 待修复 |
| 46 | High | 熔断器无并发保护：状态读取/修改非原子操作 | `circuit-breaker.ts:21-51` | 使用 `async-mutex` 的 `Mutex.runExclusive()` 确保状态转换原子性 | 待修复 |
| 47 | High | SSE 流绕过熔断器和重试 | `proxy.ts:108-122` | SSE 分支也纳入 `withCircuitBreaker` 保护，或连接失败时显式调用熔断器记录失败 | 待修复 |
| 48 | High | 熔断器 OPEN 时缺少 `Retry-After` header | `proxy.ts:154-155` | 返回 `Retry-After: 30`（与 `OPEN_TIMEOUT` 一致） | 待修复 |
| 49 | High | custom-types 路由未使用通用代理：绕过熔断器、重试、认证注入 | `custom-types/route.ts` | 重构为使用 `proxyToBackend()` 或至少使用 `fetchWithRetry()` + 熔断器 | 待修复 |
| 50 | Medium | header 过滤逻辑冗余：`forwardCookies` true/false 分支完全相同（copy-paste） | `proxy.ts:77-92` | 合并为一个分支，或在 `forwardCookies=false` 时额外过滤 `cookie` header | 待修复 |
| 51 | Medium | SSE 判断方式不可靠：仅通过 URL 以 `/stream` 结尾判断 | `proxy.ts:109-121` | 使用请求头 `Accept: text/event-stream` 或响应头 `Content-Type` 判断 | 待修复 |
| 52 | Medium | proxy-image 整体内存读取：`content-length` 缺失时无法提前拒绝大文件 | `proxy-image/route.ts:117` | 使用流式读取 + `ReadableStream.getReader()` 配合大小限制 | 待修复 |
| 53 | Medium | body 读取使用 `request.text()` 无法提前中断 | `proxy.ts:101-103` | 使用流式读取 + `TransformStream` 或 `request.clone().arrayBuffer()` 先检查大小 | 待修复 |
| 54 | Low | fetchWithRetry 对 4xx 客户端错误的处理可读性差 | `fetch.ts:13` | 添加注释说明设计意图，或对非可重试 4xx 返回结构化错误响应 | 待修复 |
| 55 | Low | 熔断器 HALF_OPEN 状态无探针限制：多个并发请求同时执行探针 | `circuit-breaker.ts:28-30` | 使用标志位或信号量，仅允许第一个请求执行探针 | 待评估 |
| 56 | Low | logger 在生产环境完全静默：无法排查线上问题 | `logger.ts` | 生产环境至少保留 `error` 级别日志，或接入结构化日志服务 | 待修复 |

---

## 07-backend-security-auth (`backend/security.py`, `backend/csrf.py`, `backend/middleware.py`)

> 审查日期: 2026-05-27

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 57 | High | 缺少 Token 撤销/黑名单机制，登出后旧 token 仍有效，JTI 已生成但未使用 | `security.py:67-75` | 创建 `revoked_tokens` 表存储已撤销的 JTI + 过期时间；`verify_token` 检查 JTI；登出时将 JTI 加入黑名单 | 待修复 |
| 58 | High | 缺少密码复杂度策略，用户可设置 1 位密码 | `security.py:30-33` | 添加 `validate_password_strength()` 函数，强制最小 8 位、包含大小写字母和数字；在注册和修改密码时调用 | 待修复 |
| 59 | High | bcrypt rounds=14 过高，高并发登录时成为性能瓶颈 | `security.py:32` | 降低到 `rounds=12`，平衡安全性和性能 | 待修复 |
| 60 | Medium | Token 签发使用 `replace(tzinfo=None)` 生成 naive datetime，跨时区处理有风险 | `security.py:47-48` | 使用 `_now.timestamp()` 存储 Unix 时间戳，或保留带时区的 datetime | 待评估 |
| 61 | Medium | `verify_token` 未验证 `iss`/`aud` claim | `security.py:67-75` | 添加 `options={"verify_aud": False}` 明确跳过，或添加 issuer/audience 验证 | 待评估 |
| 62 | Medium | Refresh Token 缺少 Rotation，旧 token 未失效 | `security.py:193-195` | 刷新后将旧 token 的 JTI 加入黑名单；使用 refresh token 专用密钥或区分 token_type | 待修复 |
| 63 | Medium | 登录锁定阈值硬编码且不一致，security.py 定义 5 但 database.py 又硬编码 `>= 5` 和 `15 分钟` | `security.py:25-26`, `database.py:515-517` | `database.py` 从 `security.py` 导入常量，或从系统配置读取 | 待修复 |
| 64 | Medium | 缺少 IP 级别登录失败追踪，攻击者可枚举多个用户名绕过锁定 | `security.py:163-178` | 添加 IP 级别失败计数表；对不存在的用户名也记录 IP 级别尝试 | 待评估 |
| 65 | Medium | CSRF Token 基于 JWT jti，XSS 泄露 JWT 时 CSRF 保护同时失效 | `csrf.py:29` | 使用独立的 CSRF token（如 `secrets.token_hex(32)`），与 JWT 分开存储 | 待评估 |
| 66 | Medium | 仅 Admin 路由使用 CSRF，前台用户路由未保护 | `main.py:1298-1535` | 对使用 cookie 认证的用户路由也添加 CSRF 保护 | 待评估 |
| 67 | Medium | 限流配置分散不可配置，阈值分散在各路由装饰器中 | `middleware.py:37` | 在 `init_rate_limiting` 中配置默认限流；或从系统配置读取 | 待评估 |
| 68 | Low | Token 过期时间 24 小时较长，Token 泄露后攻击窗口较大 | `security.py:22-23` | 考虑缩短 admin token 到 4-8 小时，或实现 sliding window 机制 | 待评估 |
| 69 | Low | 缺少密码最大长度限制，超长密码可能被用于 DoS | `security.py:36-38` | 添加 `if len(password) > 128: raise ValueError("密码长度不能超过 128 位")` | 待修复 |
| 70 | Low | 锁定时间无指数退避，每次锁定都是 15 分钟，攻击成本较低 | `database.py:515-517` | 实现指数退避（首次 15 分钟，第二次 30 分钟，第三次 1 小时等） | 待评估 |
| 71 | Low | 限流响应缺少 `Retry-After` Header，客户端无法知道何时重试 | `middleware.py:42` | 自定义异常处理器，添加 `Retry-After` header | 待修复 |
| 72 | Low | `sanitize_input` 仅 strip + 长度限制，未过滤控制字符或 Unicode 规范化 | `security.py:198-202` | 过滤控制字符；处理 Unicode 规范化；或使用 pydantic 验证 | 待评估 |
| 73 | Low | `login` 和 `login_customer` 有大量重复代码（锁定检查、密码验证、记录尝试） | `security.py:130-160,163-178` | 提取公共登录逻辑到 `_do_login(user, password, record_fn, create_token_fn)` | 待修复 |

---

## 08-backend-data-layer (`backend/database.py`)

> 审查日期: 2026-05-27

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 74 | Critical | 同步 sqlite3 调用阻塞 FastAPI 事件循环：所有 async 端点内调用同步 DB 函数，高并发时请求串行等待 | `database.py` 全文件 + `main.py` async 端点 | 短期：将调用 DB 的端点改为 `def`（非 async），让 FastAPI 自动在线程池执行；长期：引入 `aiosqlite` 将整个 DB 层改为异步 | 待修复 |
| 75 | Critical | `threading.local()` 在 asyncio 单线程下无法隔离连接：多个 async 请求共享同一 `_local.conn`，并发写入触发 `database is locked` | `database.py:13` | 改用 `contextvars.ContextVar` 管理连接上下文；或每个操作短暂获取/释放连接（不用长连接） | 待修复 |
| 76 | High | `mark_key_failed` TOCTOU 竞态：先 UPDATE fail_count、再 SELECT、再可能 UPDATE is_active，三步非原子 | `database.py:331-341` | 用单条 SQL：`UPDATE api_keys SET fail_count = fail_count + 1, is_active = CASE WHEN fail_count + 1 >= 3 THEN 0 ELSE is_active END WHERE id = ?` | 待修复 |
| 77 | High | `mark_order_paid` 手动 BEGIN/ROLLBACK 与线程本地长连接不一致：异常时 rollback 可能未执行，连接留在脏事务状态 | `database.py:694-740` | 使用 `db:` 上下文管理器或 `try/finally` 确保 rollback；或设置 `isolation_level` 让 sqlite3 自动管理事务 | 待修复 |
| 78 | High | API Key 明文存储：`key_value` 以明文存于 SQLite，数据库泄露直接暴露所有 Key | `database.py:30-31` | 存储时 AES 加密（密钥从环境变量读取），或存储 SHA-256 哈希用于查找 + 加密原文用于取用 | 待修复 |
| 79 | High | `init_db` 迁移逻辑无版本号管理：依赖 `PRAGMA table_info` 做条件迁移，无回滚策略 | `database.py:170-201` | 引入 `schema_version` 表或使用 `alembic`/`yoyo-migrations`，支持版本号 + 回滚 | 待修复 |
| 80 | Medium | `get_dashboard_stats` 执行 5-6 个独立 COUNT/AVG 全表扫描，`generation_history` 增长后变慢 | `database.py:457-481` | 合并为一个 SQL（条件聚合）；在 `created_at`、`status` 上添加索引 | 待修复 |
| 81 | Medium | 多个高频查询字段缺少索引：`generation_history.user_id`、`generation_history.status`、`orders.user_id`、`user_ledger.user_id` | `database.py` Schema 定义 | 在 `init_db` 中添加：`CREATE INDEX IF NOT EXISTS idx_history_user ON generation_history(user_id)` 等 | 待修复 |
| 82 | Medium | `INSERT OR REPLACE` 语义风险：主键冲突时 DELETE+INSERT 触发外键级联，`created_at` 默认值被重置 | `database.py:226,381` | 改用 `INSERT ... ON CONFLICT(task_id) DO UPDATE SET ...` (UPSERT) | 待修复 |
| 83 | Medium | `ensure_customer` 并发竞态：先 SELECT 再 INSERT/UPDATE 无锁保护，两个并发请求可能都执行 INSERT | `database.py:572-590` | 用 `INSERT OR IGNORE` + SELECT 模式，或 `BEGIN IMMEDIATE` + `ON CONFLICT` 处理 | 待评估 |
| 84 | Low | 单文件 940 行需拆分：所有 CRUD、Schema、迁移逻辑在一个文件，维护成本高 | `database.py` | 拆分为 `db/connection.py`、`db/schema.py`、`db/keys.py`、`db/history.py`、`db/users.py`、`db/config.py` | 待评估 |
| 85 | Low | `json` 模块重复导入：文件顶部已 import，`save_task_progress` 和 `load_pending_tasks` 内又 import | `database.py:8,224,235` | 删除函数内的重复 `import json` | 待修复 |
| 86 | Low | WAL 检查点未管理：高写入场景下 `.sqlite-wal` 文件持续增长占用磁盘 | `database.py:21` | 在 `delete_old_tasks` 等清理函数中执行 `PRAGMA wal_checkpoint(TRUNCATE)` | 待修复 |

---

## 03-frontend-admin (`src/app/admin/`)

> 审查日期: 2026-05-27

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 87 | Critical | 客户端路由守卫可被绕过：认证检查完全依赖 `useEffect`，JS 加载前或禁用 JS 时可直接访问 `/admin/*` | `layout.tsx:22-26` | 在 `middleware.ts` 层添加服务端认证检查（验证 `access_token` cookie）；至少在子页面数据获取层做 401 兜底 | 待修复 |
| 88 | Critical | 401 响应竞态：多个并发请求同时收到 401 时，`setUser(null)` 被多次调用，可能导致多次跳转 | `auth-context.tsx:79-83` | 添加全局 flag/AbortController，首次 401 时立即跳转登录页并取消后续请求；或将跳转逻辑集中到 `fetchWithAuth` | 待修复 |
| 89 | High | 登录密码无前端校验：无最小长度限制，空密码仅靠 HTML `required` 属性 | `login/page.tsx:14-20` | 在 `handleSubmit` 中添加 `if (password.length < 6)` 前置检查，配合后端防暴力破解 | 待修复 |
| 90 | High | API Key 表单无输入验证：key 格式、daily_limit 范围均未校验 | `keys/page.tsx:44-59` | 添加 key 格式校验（`length >= 10`），`daily_limit` 限制范围 `[1, 10000]` | 待修复 |
| 91 | High | LLM API Key 测试连接时明文传输：`testConnection` 将完整 key 发送到 `/api/admin/llm-config/test` | `llm/page.tsx:62-80` | 测试连接时后端直接用已存储的 key，前端只发送 `{ model }` 而非完整密钥 | 待修复 |
| 92 | Medium | Token 刷新机制未实现：前端收到 401 直接跳登录，未调用 `/admin/refresh` 续期 | `auth-context.tsx:79-83` | 401 时先尝试 `POST /admin/refresh`，成功则更新 csrfToken 并重试原请求，失败才跳登录 | 待修复 |
| 93 | Medium | 历史记录分页不支持 URL 状态：`page`/`status`/`search` 为组件内 state，刷新后丢失 | `history/page.tsx:44-46` | 使用 `useSearchParams` 或 `nuqs` 库将分页/筛选状态同步到 URL | 待修复 |
| 94 | Medium | 计费页面数据加载逻辑重复：`load()` 和 `useEffect` 内有完全相同的 `Promise.all` 逻辑 | `billing/page.tsx:46-80` | `useEffect` 中直接调用 `load()`，消除重复代码 | 待修复 |
| 95 | Medium | 删除操作确认弹窗不统一：使用原生 `confirm()` 而非项目自定义 Modal | `keys/page.tsx:67` | 统一使用 `<Modal>` 或专用 `ConfirmDialog` 组件 | 待修复 |
| 96 | Medium | 表单提交无防重复点击：异步操作期间按钮未禁用，用户可重复提交 | `keys/page.tsx:44`, `billing/page.tsx:94`, `llm/page.tsx:38` | 为每个表单操作添加 `submitting` state，按钮在请求期间 `disabled` | 待修复 |
| 97 | Low | `fetchWithAuth` Content-Type 自动设置未排除 FormData | `auth-context.tsx:70-72` | 添加 `!(options.body instanceof FormData)` 判断，避免覆盖 FormData 的 multipart 边界 | 待修复 |
| 98 | Low | 错误 catch 中 `unknown` 类型未收窄 | `history/page.tsx:63` | 使用 `e instanceof Error ? e.message : String(e)` 做类型收窄 | 待修复 |
| 99 | Low | 侧边栏菜单高亮仅支持精确匹配，子路由不高亮父菜单 | `layout.tsx:70` | 改用 `pathname.startsWith(item.path)` 或更精确的前缀匹配 | 待修复 |

---

## 05-backend-api-orchestration (`backend/main.py`)

> 审查日期: 2026-05-27

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 100 | Critical | 后台任务生命周期未管理：`asyncio.create_task` 创建的协程无引用保存，GC 后异常不可追踪；`reap_stale_tasks` 异常退出不会重启；重启后 pending tasks 加载到 task_store 但无恢复流程 | `main.py:230, 1174` | 用 `set`/`dict` 保存活跃 task 引用；reaper 添加守护重启逻辑；重启后对 pending tasks 标记 failed 或尝试恢复 | 待修复 |
| 101 | Critical | 退款竞态条件：`ensure_refund_once` 通过内存 dict 的 `refunded` 标记防重复，非原子操作，多个异常路径/协程可同时触发双重退款 | `main.py:215-219, 958-959, 1126-1136`, `database.py:807-820` | 在数据库层面实现幂等退款：`UPDATE user_ledger SET ... WHERE reference_id = ? AND type = 'refund'` + 唯一约束；或在 task_store dict 操作中加 `asyncio.Lock` | 待修复 |
| 102 | High | 全局异常处理器中 `if isinstance(exc, HTTPException): raise exc` 会再次触发自身，语义不清晰 | `main.py:159-160` | 改为 `return JSONResponse(status_code=exc.status_code, content={"detail": exc.detail})`；或移除此检查（FastAPI 默认不捕获 HTTPException） | 待修复 |
| 103 | High | `validate_image_data` 仅检查 data URL 的 MIME 声明，未校验 base64 解码后的 magic bytes，也未做图片解码验证 | `main.py:96-109` | 解码 base64 后检查文件头 magic bytes（JPEG `FF D8 FF`、PNG `89 50 4E 47`）；可选地用 Pillow 做解码验证 | 待修复 |
| 104 | High | `verify_api_auth` 使用普通字符串比较，存在时序攻击风险 | `main.py:115` | 使用 `hmac.compare_digest(auth_header, API_AUTH_TOKEN)` 进行恒定时间比较 | 待修复 |
| 105 | High | `/api/generate` 同步端点完整执行上传→LLM→14 张图轮询，耗时 2-5 分钟，阻塞 worker 连接 | `main.py:757-971` | 强制所有生成走 `/api/generate/async`；同步端点添加超时保护或降级提示 | 待修复 |
| 106 | High | SSE 端点仅验证内部 API token，不验证用户身份，任何知道 task_id 的人可订阅进度流 | `main.py:1200-1212` | 添加 `authenticate_customer` 依赖；或至少验证 `creator_ip` 与请求来源匹配 | 待修复 |
| 107 | Medium | `update_llm_config` 端点直接调用 `get_db().execute("DELETE FROM system_config ...")`，绕过抽象层 | `main.py:1523-1525` | 新增 `delete_config(key)` 函数，或统一用 `set_config("llm_api_key", "")` 清空 | 待修复 |
| 108 | Medium | `mask_api_key` 在 `main.py:1494` 和 `database.py:256` 重复定义 | `main.py:1494-1498` | 保留 `database.py` 版本，`main.py` 直接 `from database import mask_api_key` | 待修复 |
| 109 | Medium | `put /admin/api-keys/{key_id}` 接收 `req: dict` 无 Pydantic 验证，`int(req["daily_limit"])` 可能抛异常 | `main.py:1337-1351` | 定义 `UpdateApiKeyRequest` Pydantic 模型；或添加 try-except 返回 400 | 待修复 |
| 110 | Medium | `add_history()` 同步写入在生成完成后执行，写入失败时生成结果丢失 | `main.py:908-914, 1103-1122` | 异步写入历史记录；或在 `add_history` 中添加重试和降级逻辑 | 待评估 |
| 111 | Low | `@app.on_event("startup")` 已在 FastAPI 0.93+ 废弃 | `main.py:173` | 迁移到 `@asynccontextmanager async def lifespan(app): ...` | 待修复 |

---

## 06-backend-image-generation-pipeline (`backend/prompts_v2.py`, `backend/llm_provider.py`, `backend/llm_schema.py`, `backend/llm_prompts.py`)

> 审查日期: 2026-05-27

| # | 严重程度 | 问题 | 文件:行号 | 修复建议 | 状态 |
|---|----------|------|-----------|----------|------|
| 112 | Critical | Prompt 注入黑名单仅 10 个英文关键词，中文注入、编码绕过、复合注入均可绕过；截断策略过于宽松 | `prompts_v2.py:7-12, 15-26` | 扩展黑名单（添加中文关键词）；改为拒绝请求而非截断；对 LLM 输出做二次验证 | 待修复 |
| 113 | High | `LLMOutput` Pydantic schema 已定义但从未使用，LLM 返回原始 dict 未校验 | `llm_provider.py:108-111`, `llm_schema.py` | 在 `analyze()` 中添加 `LLMOutput.model_validate(result)` 验证，失败返回 None 触发降级 | 待修复 |
| 114 | High | LLM 超时 30 秒可能不足，且无重试机制，临时网络故障直接降级 | `llm_provider.py:17, 89-124` | 超时增加到 45-60 秒；对 `TimeoutException`/`ConnectError` 添加 1-2 次指数退避重试 | 待修复 |
| 115 | Medium | `match_category` 按固定优先级匹配，"运动鞋"匹配 `fashion_shoes` 而非 `sports_equipment` | `prompts_v2.py:404-427` | 改为最长关键词匹配优先；或增加匹配置信度评分 | 待评估 |
| 116 | Medium | `prompts_v2.py` 892 行包含品类/模型/国家配置、prompt 构建、任务编排，职责过多 | `prompts_v2.py` 全文 | 拆分为 `categories.py`、`models.py`、`countries.py`、`prompt_builder.py`、`task_generator.py` | 待修复 |
| 117 | Medium | 配置全部硬编码在 Python 字典中，新增国家或品类需要改代码重启服务 | `prompts_v2.py:33-615` | 外部化为 JSON/YAML 配置文件；支持热加载 | 待评估 |
| 118 | Medium | `build_llm_messages` 和 `analyze` 存在重复 image_url 处理逻辑，messages 被构建两次 | `llm_provider.py:26-63, 84-87` | 让 `analyze()` 接受预构建的 messages 参数 | 待修复 |
| 119 | Low | `sanitize_prompt_input` 截断到注入关键词位置，用户无法感知输入被修改 | `prompts_v2.py:24` | 改为拒绝整个请求并返回明确错误提示 | 待评估 |
| 120 | Critical | LLM 输出 JSON 字段（titles/description/scenes）无 XSS 过滤，直接透传前端渲染 | `llm_provider.py:110-111` | 对 LLM 返回的字符串字段做 HTML 转义；过滤 `<script>`/`javascript:` 等危险内容；存入数据库前做内容安全检查 | 待修复 |
| 121 | High | LLM 降级未区分临时故障与永久故障（如 API Key 无效/模型不存在），每次请求白白尝试 | `llm_provider.py:113-123`, `main.py:822-823` | 区分可恢复错误（超时、5xx）和不可恢复错误（401/403/模型不存在）；对不可恢复错误设置熔断（如 5 分钟内不再尝试） | 待修复 |
| 122 | High | `json.loads` 未处理 LLM 返回带 markdown code fence（` ```json...``` `）的情况 | `llm_provider.py:110` | 在 `json.loads` 前清理：去除首尾空白、检测并剥离 code fence 包裹 | 待修复 |
| 123 | High | `sanitize_prompt_input` 消毒入口不统一：`generate_all_tasks` 未消毒直接传给各 prompt builder，每个 builder 内部重复调用 | `prompts_v2.py:633,709,734,757`, `generate_all_tasks` | 在 `generate_all_tasks` 入口处统一消毒一次，下游函数信任已消毒的输入 | 待修复 |
| 124 | Medium | 国家配置 `model_desc` 涉及具体种族/外貌描述（肤色、身高、体型），可能产生刻板印象 | `prompts_v2.py:527-614` | 评估是否可使用更通用、不涉及具体外貌特征的描述，或确保描述尊重多元审美 | 待评估 |
| 125 | Medium | LLM 覆盖 `suggested_shot_type` 仅对 `parent=general` 生效，穿戴类品类无法被 LLM 纠正 shot_type | `prompts_v2.py:846-856` | 对穿戴类品类也允许 LLM 覆盖 shot_type（至少 `product→model` 方向），或增加置信度阈值 | 待评估 |
| 126 | Low | `llm_provider.py` 全部使用 `print()` 而非 `logging`，日志无法统一管理 | `llm_provider.py:105,114,117,120,123` | 改用 `logger = logging.getLogger("ecommerce-gen.llm")`，使用 `logger.warning`/`logger.error` | 待修复 |
| 127 | Low | `FRONTEND_MODEL_MAP` 中 `portrait`/`fashion`/`viral` 三个前端码都映射到 `flux`，用户选择不同风格但 AI 模型相同 | `prompts_v2.py:492-498` | 确认前端是否用这三个码表示不同风格但同模型；如果是，应在前端明确说明 | 待评估 |
